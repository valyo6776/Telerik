using System;

namespace PrintSequence
{
    class PrintSequence
    {
        public static void Main()
        {
            for (int i = 2; i < 12; i++)
            {
                Console.WriteLine(i % 2 == 1 ? -i : i);
            }
        }
    }
}
